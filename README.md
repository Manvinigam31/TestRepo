# Selenium Entrata Tests

This project contains Selenium tests for the website entrata.com using Java, JUnit, and Maven.

## Prerequisites
- Java Development Kit (JDK) installed
- Maven installed
- All the possible tests covered in EntrataTests.java class

## Running the Tests


3. Run the following command to execute the tests:
   ```bash
   mvn verify
